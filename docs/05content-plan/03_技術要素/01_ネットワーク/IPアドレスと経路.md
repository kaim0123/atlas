# ネットワーク｜IPアドレスと経路

> 分割元: [`00_分割元_ネットワーク基礎.md`](../00_分割元_ネットワーク基礎.md) §2
> 対応ページ（実装済み）: [`/network/ip`](../../../../src/app/network/ip/page.tsx)

端末の住所づけ（IPアドレス）と、インターネットへの出口（NAT・DNS）を整理します。

---

## 2. IPアドレスと経路

### 2.1 IPアドレス

- **IPv4**: 32ビット。例）192.168.1.1。アドレス不足のためIPv6へ移行中。
- **グローバルIP**: インターネット上で一意。
- **プライベートIP**: 組織内だけで一意。インターネットにはそのまま出せない。

### 2.2 サブネットマスクとCIDR

- **サブネットマスク**: ネットワーク部とホスト部の境目を示す。例）255.255.255.0。
- **CIDR表記**: 例）192.168.1.0/24 → 先頭24ビットがネットワーク部。

### 2.3 NATとDNS

- **NAT**: プライベートIPの端末がインターネットにアクセスするとき、送信元をグローバルIPに書き換える。少ないグローバルIPで多数の端末を接続できる。
- **DNS**: ドメイン名（www.example.com）からIPアドレスを調べる**名前解決**を行う。

---

## この先どこで使うか

- 階層モデルの前提 → [`階層モデル.md`](階層モデル.md)（`network/layers/`）
- ファイアウォール・VPN の防御側 → `security/countermeasures/`
- DNS の Web 配信・CDN 詳細 → `internet/dns/`
