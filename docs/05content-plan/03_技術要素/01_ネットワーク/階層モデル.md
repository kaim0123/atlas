# ネットワーク｜階層モデル

> 分割元: [`00_分割元_ネットワーク基礎.md`](../00_分割元_ネットワーク基礎.md) §1
> 対応ページ: `network/layers/`

ネットワークを役割ごとに**階層**に分けて設計する考え方と、OSI 7層・TCP/IP 4層の対応を整理します。

---

## 1. 階層モデルの考え方

ネットワークは役割ごとに**階層**に分けて設計します。各層の役割が明確になり、変更の影響が局所化できます。

### 1.1 OSI基本参照モデル（7層）

| 層 | 名前 | 主な役割 |
|----|------|----------|
| 7 | アプリケーション層 | HTTP、メールなど |
| 6 | プレゼンテーション層 | データ形式・変換・暗号化 |
| 5 | セション層 | 通信の開始・終了・同期 |
| 4 | トランスポート層 | TCP/UDP、端末間の通信 |
| 3 | ネットワーク層 | 経路選択、IPアドレス |
| 2 | データリンク層 | 隣接機器間のフレーム、MAC |
| 1 | 物理層 | 電気・光の信号、ケーブル |

### 1.2 TCP/IPの4層

| 階層 | 主なプロトコル |
|------|----------------|
| アプリケーション層 | HTTP/HTTPS、SMTP、POP、DNS、DHCP、FTP |
| トランスポート層 | **TCP**（信頼性）、**UDP**（軽量） |
| インターネット層 | **IP**、ICMP、ARP |
| ネットワークインターフェース層 | イーサネット、無線LAN |

---

## この先どこで使うか

- 各層の住所づけ（IP・サブネット・NAT・DNS）→ [`IPアドレスと経路.md`](IPアドレスと経路.md)（`network/ip/`）
- トランスポート層の TCP/UDP・ポート番号 → [`TCPとUDP.md`](TCPとUDP.md)（`network/transport/`）
