# データベース｜トランザクションとACID

> 分割元: [`00_分割元_データベース基礎.md`](../00_分割元_データベース基礎.md) §4
> 対応ページ: `database/transaction/`

まとめて成功するか取り消すか、という単位（トランザクション）と、その保証（ACID）を整理します。

---

## 4. トランザクションとACID

**トランザクション**は「まとめて成功するか、まとめて取り消すか」の単位です。

### 4.1 ACID特性

| 頭文字 | 意味 |
|--------|------|
| **A**tomicity（原子性） | 全体が実行されるか、全体が取り消されるか |
| **C**onsistency（一貫性） | 制約を満たした状態が保たれる |
| **I**solation（独立性） | 並行トランザクションが互いに不適切に影響しない |
| **D**urability（永続性） | コミットした結果は障害後も失われない |

### 4.2 排他制御と障害回復

- **ロック**: 更新前にロックをかけ、専有ロック・共有ロックで整合性を守る。**デッドロック**に注意。
- **ログ**: 更新前後の値や操作を記録。**ロールバック**（取り消し）や**ロールフォワード**（再実行）に使う。
- **バックアップ**: フル、差分、増分の使い分けが重要。

---

## この先どこで使うか

- ER図と正規化 → [`ER図と正規化.md`](ER図と正規化.md)（`database/design/`）
- 索引・RAID・レプリケーションなど物理設計実務 → `dev/database/physical/`
- 開発の流れ（要件定義からテストまで）→ 開発技術セクション（未計画）
