# データベース｜役割と種類

> 分割元: [`00_分割元_データベース基礎.md`](../00_分割元_データベース基礎.md) §1
> 対応ページ（実装済み）: [`/database/basics`](../../../../src/app/database/basics/page.tsx)
>
> **`/database/*` と `/dev/database` の使い分け**: e-Words 技術要素の試験・初学者向けの土台は **`/database/*`**（basics・model・design・sql・transaction・advanced）に置く。開発者向けの追補（物理設計・運用の実務、歴史）は **`/dev/database/*`**（physical・history）に残し、`/dev/database` トップは両者をつなぐハブとする。

なぜファイルではなく DBMS を使うのか、その役割を整理します。

---

## 1. データベースの役割

- データを**一箇所で管理**し、重複や不整合を減らす。
- **複数ユーザ**が同時にアクセスしても、矛盾のない形で共有できる。
- **障害**に備えた回復機能（バックアップ、ログ、トランザクション）を提供する。
- **検索・集計**をSQLで効率よく行える。

ファイルに書き込むだけでは同時アクセス制御や整合性の保証が難しいため、**DBMS**を介してデータを扱います。

---

## この先どこで使うか

- 表でデータを表す関係モデルと3層スキーマ → [`関係モデルと3層スキーマ.md`](関係モデルと3層スキーマ.md)（`database/model/`）
- まとめて成功／取り消しするトランザクション → [`トランザクションとACID.md`](トランザクションとACID.md)（`database/transaction/`）
